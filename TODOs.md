# TODOs

# Features/Enchancements
- [x] When a chore is accepted/rejected by a parent this information should be visible in the activity table: Which parent accepted/rejected it.
- [x] the site is a bit slow my older tablet. Is there a way to optimise it? (removed animated full-screen `filter: blur()` backgrounds → GPU-cheap radial gradients; AuthGuard no longer re-checks the session on every navigation)
