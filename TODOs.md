# TODOs

- [x] for chores that repeat daily, the day starts at 00.00, for chores that repeat weakly, week starts on Monday 00.00. Check the code and the db queries and verify the system works like this.
    - Verified: `_period_bounds`/`_start_of` in `backend/app/services/chores.py` scope daily chores to the Athens 00:00→00:00 day and weekly (`repeat_days`) chores to Mon 00:00→next Mon 00:00; bounds are emitted as naive-UTC to match the stored naive-UTC ledger/claim timestamps.
    - [x] Chores that are for "each kid" should reset on their repeat time per kid — `_get_claim_status` filters claims/approvals by `user_id` for `claim_mode="each"`.
    - [x] Chores that are for "One kid" should reset similarly but also make sure that no other kid has claimed or completed this chore during the period — `claim_mode="one"` checks any user's `PendingClaim` *and* any user's `chore_approved` ledger row within the period.
    - [x] implement tests that verify this behaviour — added daily per-kid reset, daily one-mode cross-kid block/reset, one-mode blocked-by-completion, and the Athens-midnight boundary tests in `backend/tests/test_phase4.py`.
    - [x] A chore's recorded time is the **claim** time, not the approval time — `approve_claim`/`decline_claim` in `backend/app/services/approvals.py` now set the `HistoryLedger.timestamp` to `claim.claimed_at` instead of letting it default to `func.now()`. This makes period bucketing follow when the kid actually did the work (a claim made late in one period but approved in the next still counts toward the claim period). `retroactive_decline` is intentionally left at "now" (it's a new reversal event). Covered by `test_approved_ledger_uses_claim_time_not_approval_time` (83 backend tests pass).
    - NOTE (not changed): the `claim_mode="one"` race guard in `backend/app/api/dashboard.py` (`claim_chore`) still uses single-day bounds instead of `_period_bounds`; this is redundant because the preceding `chore_is_available_for` check already enforces the full period, but worth tidying for consistency.
