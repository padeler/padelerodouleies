# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
htmlcov/
coverage.xml
.coverage

# Backend venv
backend/.venv/
backend/data/

# Frontend
frontend/node_modules/
frontend/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Claude
.claude/
venv
# NOTE: backend/app/icons/svg is intentionally tracked. The SVGs are baked into
# the image for reproducible, CDN-independent builds (LAN-only deploy). Keep
# this in sync with the icon catalog; fetch_icons.py is a dev-only top-up tool.
.playwright-mcp/
data
frontend/test-results

# Local Piper TTS voice models (fetched via backend/scripts/fetch_voices.sh)
backend/voices/
# Autogenerated sample exercise bundles
samples/
